# secrets — never commit
.env
.env.production
.env.server
.env.local

# recovered prior-build reference (not part of the project)
reference/

# local artifacts
node_modules/
*.log
.DS_Store
.env.deploy
.claude/

# mutation testing of our own code: sandbox copies and reports are build output
.stryker-tmp/
reports/
.stryker-tmp-*/
